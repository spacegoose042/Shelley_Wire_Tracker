DATABASE_URL="postgresql://user:password@localhost:5432/shelley_wire_tracker"
NEXTAUTH_SECRET="generate-a-long-random-string-for-production"
NEXTAUTH_URL="http://localhost:3000"
# Set AUTH_TRUST_HOST=true in production (e.g. Railway) when behind a proxy
# AUTH_TRUST_HOST=true
# One-time setup: set a secret, then visit /api/setup?secret=YOUR_SECRET to create admin user
# SETUP_SECRET=your-random-secret
